import styled from "styled-components";

const Container = styled.div`
  padding: 20px;
`;

export default function Scheduling() {
  return (
    <Container>
      <h1>统一调度面板</h1>
      <p>监控所有系统的运行状态。</p>
    </Container>
  );
}
